# Training Requirements
#
# For GPU training (recommended):
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu124
#
# For CPU-only training:
#   pip install -r requirements.txt

# Basic dependencies for downloading and labeling
requests>=2.32.0
pillow>=10.0.0

# ML training dependencies
# PyTorch with CUDA support - use --index-url https://download.pytorch.org/whl/cu124
# Note: torchvision not needed (removed to reduce install size)
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0
tqdm>=4.65.0
scikit-learn>=1.3.0

# Note: tkinter required for label_images.py GUI
# macOS: brew install python-tk@3.13
# Ubuntu/Debian: sudo apt-get install python3-tk
