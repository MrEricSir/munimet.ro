# Scripts Requirements
# Install with: pip install -r scripts/requirements.txt

# OpenCV for image processing
opencv-python>=4.8.0

# PIL for image display in tkinter
pillow>=10.0.0

# NumPy for array operations
numpy>=1.24.0

# Flask for web-based visualization
flask>=3.0.0

# Tesseract OCR for train ID detection
# Note: Requires Tesseract installed on system (brew install tesseract on macOS)
pytesseract>=0.3.10
