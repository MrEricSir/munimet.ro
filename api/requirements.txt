# API Requirements
# Install with: pip install -r requirements.txt

# Web framework and server
falcon>=4.0.0
gunicorn>=23.0.0
whitenoise>=6.0.0

# Image processing and HTTP
requests>=2.32.0
pillow>=10.0.0

# Cloud storage (for Cloud Run deployment)
google-cloud-storage>=2.10.0

# ML dependencies for prediction (CPU-only for faster builds)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0

# Bluesky integration
atproto>=0.0.55

# Environment management
python-dotenv>=1.0.0
